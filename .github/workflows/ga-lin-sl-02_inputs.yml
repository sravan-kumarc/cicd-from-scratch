name: LIN2 - Pipeline with Inputs

on:
  workflow_dispatch:
    inputs:
      message:
        description: 'Message to print'
        required: true
        default: 'Simple job!'

jobs:
  hello:
    runs-on: ubuntu-latest

    steps:
      - name: Print message
        run: echo "--> ${{ inputs.message }}!"
