name: LIN13 - Git Leak Detection
on:
  [push, pull_request,workflow_dispatch]

jobs:
  git-leak-detection:
    name: Git Leak Detection_job
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v6
        with:
         fetch-depth: 0  # Fetch all history for git leak detection
      - uses: gitleaks/gitleaks-action@v2
        with:
          AWS_ACCESS_KEY: ${{ secrets.AWS_ACCESS_KEY }}
          AWS_SECRET_KEY: ${{ secrets.AWS_SECRET_KEY }}
    
            # Example of passing a secret to gitleaks