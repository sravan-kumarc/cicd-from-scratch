apiVersion: v1
kind: Service
metadata:
  labels:
    app: dep-base
  name: dep-base
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 5000
  selector:
    app: dep-base
  type: LoadBalancer
